package build.runner.eclipse

import mill.*
import millbuild.*

object `package` extends MillPublishScalaModule {
  def moduleDeps = Seq(build.libs.util, build.core.internal)
  def mvnDeps = Seq(Deps.scalaXml)
}
