package build.runner.idea

import mill.*
import millbuild.*

object `package` extends MillPublishScalaModule {
  def moduleDeps = Seq(build.libs.util, build.libs.script)
  def mvnDeps = Seq(Deps.scalaXml, Deps.jgit)
}
