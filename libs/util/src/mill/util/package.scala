package mill

/**
 * Non-language-specific utility APIs provided by Mill for use in your
 * `build.mill` and `package.mill`, in addition to the core APIs provided in
 * the `mill.api` package
 */
package object util
