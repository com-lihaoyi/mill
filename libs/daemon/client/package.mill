package build.libs.daemon.client

import mill.*
import millbuild.*
import mill.scalalib.*

/**
 * This package contains the code for the Mill-specific TCP client implementation.
 */
object `package` extends MillPublishScalaModule with MillJava11ScalaModule {
  def moduleDeps = Seq(build.core.constants)
  def mvnDeps = Seq(Deps.osLib, Deps.upickle)
}
