package build.libs.daemon.server

import mill.*
import millbuild.*

/**
 * This package contains the code for the Mill-specific TCP server implementation.
 */
object `package` extends MillPublishScalaModule {
  def moduleDeps = Seq(build.libs.daemon.client, build.core.api.daemon)

  def scalaVersion = "3.7.4"
  def mvnDeps = Seq(Deps.osLib)
}
