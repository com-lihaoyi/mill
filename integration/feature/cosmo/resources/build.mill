package build

import mill._, javalib._, scalalib._

object hello extends JavaModule with cosmo.CosmoModule
object javaopts extends JavaModule with cosmo.CosmoModule {
  def forkArgs = Seq("-Dmy.java.property=hello");

  override def forkArgs(argv0: String) = Task.Anon {
    Seq(s"-Dmy.argv0=$argv0")
  }
}
