//| mill-repositories: [$PWD_URI/custom-local-repo/]

package build

import mill._, javalib._

object foo extends JavaModule {
  def mvnDeps = Seq(
    mvn"com.lihaoyi:mill-core-constants:SNAPSHOT"
  )
}
