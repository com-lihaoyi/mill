package build
import mill._

val valueFoo = 0
val valueFooUsedInBar = 0
lazy val lazyValue = 0
lazy val lazyValue2 = 0
def helperFoo = { println("running helperFoo"); 1 + valueFoo + lazyValue }
def foo = Task { println("running foo"); helperFoo + lazyValue }
