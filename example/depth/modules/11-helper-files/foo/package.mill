package build.foo
import mill._, scalalib._

object `package` extends RootModule with build_.util.MyModule {
  def forkEnv = Map(
    "MY_SCALA_VERSION" -> build_.util.myScalaVersion,
    "MY_PROJECT_VERSION" -> myProjectVersion
  )
}
