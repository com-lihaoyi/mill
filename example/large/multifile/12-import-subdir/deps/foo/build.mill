// deps/foo/build.mill
// Demonstrates relaxed package naming: we use `package build` instead of
// the directory-based `package build.deps.foo` for simplicity
package build

import mill.*, scalalib.*

trait FooModule extends ScalaModule {
  def scalaVersion = "2.13.16"
}

object `package` extends FooModule