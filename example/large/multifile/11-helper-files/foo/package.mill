package build.foo

import mill.*, scalalib.*
import mill.api.opt.*

object `package` extends build.MyModule {
  def forkEnv = Map(
    "MY_SCALA_VERSION" -> Opt(build.myScalaVersion),
    "MY_PROJECT_VERSION" -> Opt(myProjectVersion)
  )
}
