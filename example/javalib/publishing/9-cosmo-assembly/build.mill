package build
import mill._, javalib._

object foo extends JavaModule with cosmo.CosmoModule
// This example uses the `scalalib.cosmo.CosmoModule` to build a cross-platform executable assembly by prepending
// a launcher in the Actually Portable Executable (APE) format compiled with the Cosmopolitan's toolchain.

/** Usage

> ./mill show foo.assembly # mac/linux

> ./out/foo/assembly.dest/out.jar.exe # mac/linux
Hello, World!

*/
