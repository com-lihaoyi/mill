Todo: Part A
   step: 0 Fix wrong tsconfig outputs ✅
   include test files in tsconfig ✅
   fix upstreamPathsBuilder <include recModDeps> ✅
   in `trait Shared` return only test specific upstream tsconfig ✅
   combing with origin upstreams :(  ✅
   combine compilerOptions with testCompilerOptions ✅
   `test/**/utils`  ✅
   in tsconfig, add [ts-path configurations]
                             - "test/src/utils"
                             - "test/utils"
           - install to outer/npmInstall ✅
           - create tsconfig for testing in test/compileDir
                - `package` use fix:
                     - fix the inclusion of '*'  ✅
                     - fix  /test/utils           ✅
           - Handle case when test does not have a src directory  ✅
           - nested module deps not working as expected => use recursive retrival of dependencies ✅
           - fix "test/src/utils" => "test/**/utils" ✅
   symlink compileDir<outer> to compilerDir<test> <- run tests from here :)
   compileDir<outer> (tests will be included)  ✅
           - symlink files in compileDir from outer to test/compileDir
              - used copy for reasons with jest not finding coverage files
           - run tests from <out/test/compile.dest/typescript>
   Step: 1 => make PublishModule fully depend on TypescriptModule ✅
   Step: 2 => Fix TscBundle & PubBundle ✅
   Step: 3 => Focusing on named modules && Get All previous test passing [current]
   notes: Generated sources and resources in pub module need to be moved into compilerDir() ✅
            - move over-ridden sources + tscAllSources() ✅
                - Handle copying mod-deps sources in out/ (mod deps sources in sources.dest) ✅
            - move generated sources ✅ + tscAllSources() ✅<rootDir>/typescript/generatedSources
               - move mod dependencies generated sources <rootDir>/typescript/<mod>/generatedSources ✅
            - move resources in out/ ✅
                 - core
                 - mod deps
   Step: 3(1)
            - Problem => jest config uses root <rootDir>/typescript:
                Temp Fix: copy resources to <rootDir>/typescript/resources ✅
                Fix: use symlink instead of copy
            - "@test/resources/" is missing from tsconfig ✅

#################################################################################

Todo:
   Step: 4 => Fix `package` use:
              - basic, module ✅
              - testing ✅
``
#################################################################################

Todo:
    Step: 5 => Include Esm Support
    Step: 6 => Migrate Ollama-js

#################################################################################


Todo: <<package root module>>
   - make default name package on empty super.toString
   - handle edge case tsconfig gen
         - moduleName and this.toString does not match

Todo: Resources
     - mv all external resources to typescript/
     - if a resource does not exist in millSourcePath